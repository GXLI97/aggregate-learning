#!/bin/bash
#SBATCH --partition=gpu --cpus-per-task=1 --array=1-12
eval "$($HOME/mc3/bin/conda 'shell.bash' 'hook')"
. ~/myenv/bin/activate
bash -c "`sed "${SLURM_ARRAY_TASK_ID}q;d" batch-commands-last.txt`"